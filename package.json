{
  "name": "shift-happens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": { "test": "node tests/run.mjs", "start": "python -m http.server 8000 --bind 127.0.0.1" },
  "engines": { "node": ">=20" }
}
